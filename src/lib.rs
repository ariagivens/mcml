mod assign_homes;
mod datapack;
mod lex;
mod linearize;
mod parse;
mod select_commands;
mod utility;

use anyhow::Result;
pub use datapack::Datapack;
use lex::lex;
use parse::parse;
use select_commands::select_commands;
use assign_homes::assign_homes;
use linearize::linearize;

pub fn compile(source: &str) -> Result<Datapack> {
    let functions = select_commands(assign_homes(linearize(parse(lex(source)?)?)))?;

    Ok(Datapack {
        description: "Datapack generated by MCML".to_owned(),
        pack_format: 18,
        functions,
    })
}
