mod assign_homes;
mod datapack;
mod emit_text;
mod reify_locations;
mod lex;
mod linearize;
mod parse;
mod runtime;
mod select_instructions;
mod uniquify;
mod utility;
mod var;

use anyhow::Result;
use assign_homes::assign_homes;
pub use datapack::Datapack;
use emit_text::emit_text;
use lex::lex;
use linearize::linearize;
use parse::parse;
use select_instructions::select_instructions;
use uniquify::uniquify;
use reify_locations::reify_location;

pub fn compile(source: &str) -> Result<Datapack> {
    let functions = emit_text(reify_location(assign_homes(select_instructions(linearize(uniquify(
        parse(lex(source)?)?,
    ))))))?;

    Ok(Datapack {
        description: "Datapack generated by MCML".to_owned(),
        pack_format: 18,
        functions,
    })
}
